name: 干潟監視システム（潮汐データ統合版）

on:
  repository_dispatch:
    # 2つの合言葉を受け付けるようにします
    types: [trigger-daily-tide, trigger-monitor]
  workflow_dispatch:

jobs:
  # ジョブ1: 潮汐データ更新（1日1回）
  update-tide-data:
    runs-on: ubuntu-latest
    # 合言葉が「trigger-daily-tide」の時だけ実行
    if: github.event.action == 'trigger-daily-tide' || github.event_name == 'workflow_dispatch'
    
    steps:
    - name: リポジトリをチェックアウト
      uses: actions/checkout@v4 # v4推奨
      with:
        token: ${{ secrets.NAYACAMERA }} # Push権限のため

    - name: Pythonセットアップ
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: 依存関係をインストール
      run: |
        pip install requests beautifulsoup4
        
    - name: 潮汐データを取得
      run: |
        python fetch_tide_data.py
        
    - name: 変更をプッシュ
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git add tide_prediction.json
        
        if git diff --staged --quiet; then
          echo "変更なし"
          exit 0
        fi
        
        git commit -m "🌊 潮汐データ更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d')"
        git push

  # ジョブ2: 干潟監視（15分ごと）
  monitor-tidal-flat:
    runs-on: ubuntu-latest
    # 合言葉が「trigger-monitor」の時だけ実行
    if: github.event.action == 'trigger-monitor' || github.event_name == 'workflow_dispatch'
    
    steps:
    - name: リポジトリをチェックアウト
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.NAYACAMERA }}

    - name: Pythonセットアップ
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: 依存関係をインストール
      run: |
        pip install requests beautifulsoup4 opencv-python-headless numpy
        
    - name: 干潟解析を実行
      run: |
        python monitor_tidal_flat.py
        
    - name: 変更をプッシュ
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git add results/
        
        if git diff --staged --quiet; then
          echo "変更なし"
          exit 0
        fi
        
        git commit -m "🌊 解析結果更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M:%S JST')"
        git push
