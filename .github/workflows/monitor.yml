name: 干潟監視システム

on:
  schedule:
    # きっちり毎時00分と30分に実行 (UTCで指定)
    # 日本時間 6:00, 6:30, 7:00, 7:30, ..., 18:00, 18:30
    # = UTC 21:00, 21:30, 22:00, 22:30, 23:00, 23:30, 0:00, 0:30, ..., 9:00, 9:30
    
    # JST 6:00-8:30
    - cron: '0 21 * * *'   # JST 6:00
    - cron: '30 21 * * *'  # JST 6:30
    - cron: '0 22 * * *'   # JST 7:00
    - cron: '30 22 * * *'  # JST 7:30
    - cron: '0 23 * * *'   # JST 8:00
    - cron: '30 23 * * *'  # JST 8:30
    
    # JST 9:00-18:30
    - cron: '0 0 * * *'    # JST 9:00
    - cron: '30 0 * * *'   # JST 9:30
    - cron: '0 1 * * *'    # JST 10:00
    - cron: '30 1 * * *'   # JST 10:30
    - cron: '0 2 * * *'    # JST 11:00
    - cron: '30 2 * * *'   # JST 11:30
    - cron: '0 3 * * *'    # JST 12:00
    - cron: '30 3 * * *'   # JST 12:30
    - cron: '0 4 * * *'    # JST 13:00
    - cron: '30 4 * * *'   # JST 13:30
    - cron: '0 5 * * *'    # JST 14:00
    - cron: '30 5 * * *'   # JST 14:30
    - cron: '0 6 * * *'    # JST 15:00
    - cron: '30 6 * * *'   # JST 15:30
    - cron: '0 7 * * *'    # JST 16:00
    - cron: '30 7 * * *'   # JST 16:30
    - cron: '0 8 * * *'    # JST 17:00
    - cron: '30 8 * * *'   # JST 17:30
    - cron: '0 9 * * *'    # JST 18:00
    - cron: '30 9 * * *'   # JST 18:30
    
  workflow_dispatch: # 手動実行も可能

jobs:
  monitor-tidal-flat:
    runs-on: ubuntu-latest
    
    steps:
    - name: リポジトリをチェックアウト
      uses: actions/checkout@v3
      
    - name: Pythonセットアップ
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 依存関係をインストール
      run: |
        pip install requests beautifulsoup4 opencv-python-headless numpy
        
    - name: 干潟解析を実行
      run: |
        python monitor_tidal_flat.py
        
    - name: 解析結果をコミット
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add results/
        git diff --quiet && git diff --staged --quiet || git commit -m "🌊 解析結果更新: $(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M:%S JST')"
        
    - name: 変更をプッシュ
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
